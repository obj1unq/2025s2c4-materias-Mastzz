import ejercicio.*

describe "Inscripción a materias" {
    method initialize() {
        const property programacion = new Carrera()
        const property medicina = new Carrera()
        const property derecho = new Carrera()

        const roque = new Estudiante()
        roque.inscribirEnCarreras(#{})
    }
  test "" {
    
  }
}